<h1>id:<%= @task.id %>のタスクの編集</h1>

<%= form_for (@task) do |f| %>
 <%= f.label :content, "タスク" %>
 <%= f.text_field :content %>
 
 <%= f.label :status, "ステータス" %>
 <%= f.text_field :status %>
 
 <%= f.submit "送信" %>
<% end %>

<%= link_to "一覧に戻る",tasks_path %>